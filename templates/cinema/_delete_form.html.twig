<form method="post" action="{{ path('app_cinema_delete', {'id': cinema.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cinema.id) }}">
    <button class="btn bg-red-500 hover:bg-red-600 text-white rounded-md px-4 py-2">Delete</button>
</form>
