{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class=" bg-blue-600 text-white mt-2 py-2 px-3 rounded-lg hover:bg-blue-700 transition duration-300">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
